import React from 'react';
import { useDispatch, useSelector } from 'react-redux';

export default TelOneTownOptions = ({name, id, email}) => {
    return (
        <>
            {name}
        </>
        
    )
}
