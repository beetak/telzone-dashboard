import React from 'react';
import { useDispatch, useSelector } from 'react-redux';

export default TelOneRegionOptions = ({name, id, email}) => {
    return (
        <>
            {name}
        </>
        
    )
}
